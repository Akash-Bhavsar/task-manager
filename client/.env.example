# Client environment variables. Copy to `.env.local` and fill in real values.

# Base URL of the backend API (no trailing slash).
# Local: http://localhost:4000 (the API; the client dev server itself runs on 3000)
NEXT_PUBLIC_API_URL=
